<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title>🌹</title>
<style>
body {
margin: 0;
height: 100vh;
display: grid;
place-items: center;

/* Pastel background that complements rose */
background: radial-gradient(circle at top left, #ffe6f2 0%, transparent 45%),
radial-gradient(circle at top right, #e9ddff 0%, transparent 45%),
radial-gradient(circle at bottom left, #fff1db 0%, transparent 55%),
radial-gradient(circle at bottom right, #ffd6e8 0%, transparent 55%),
linear-gradient(180deg, #fff6fb 0%, #f7f0ff 50%, #fff3e8 100%);

overflow: hidden;
font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial;
}

/* dreamy blur blobs */
.blob {
position: absolute;
width: 420px;
height: 420px;
border-radius: 999px;
filter: blur(70px);
opacity: 0.35;
animation: blobMove 10s ease-in-out infinite;
z-index: 0;
}
.blob.one { background: #ff5fa2; top: -140px; left: -140px; }
.blob.two { background: #cdb4ff; bottom: -160px; right: -160px; animation-delay: 2s; }
.blob.three { background: #ffd6a5; top: 40px; right: -200px; animation-delay: 4s; }

@keyframes blobMove {
0%,100% { transform: translate(0,0) scale(1); }
50% { transform: translate(35px, -25px) scale(1.08); }
}

.card {
position: relative;
width: min(600px, 92vw);
padding: 34px 22px 26px;
border-radius: 28px;

/* pastel glass look */
background: rgba(255,255,255,0.55);
border: 1px solid rgba(255, 105, 180, 0.18);
backdrop-filter: blur(14px);

box-shadow: 0 22px 70px rgba(0,0,0,0.12);
text-align: center;
z-index: 2;
}

h1 {
margin: 0 0 6px;
color: rgba(40, 10, 25, 0.9);
font-size: 22px;
letter-spacing: 0.4px;
font-weight: 850;
}

p {
margin: 0 0 18px;
color: rgba(60, 20, 40, 0.65);
font-size: 14px;
font-weight: 600;
}

/* floating sparkles */
.sparkle {
position: absolute;
width: 7px;
height: 7px;
border-radius: 999px;
background: rgba(255, 120, 190, 0.8);
box-shadow: 0 0 16px rgba(255, 120, 190, 0.55);
animation: floatUp 6s linear infinite;
opacity: 0.8;
z-index: 1;
}

@keyframes floatUp {
0% { transform: translateY(40px) scale(0.7); opacity: 0; }
12% { opacity: 0.9; }
100% { transform: translateY(-820px) scale(1.2); opacity: 0; }
}

/* hearts */
.heart {
position: absolute;
font-size: 18px;
animation: heartFloat 7s linear infinite;
opacity: 0.85;
filter: drop-shadow(0 8px 10px rgba(0,0,0,0.12));
z-index: 1;
user-select: none;
}

@keyframes heartFloat {
0% { transform: translateY(50px) scale(0.8) rotate(0deg); opacity: 0; }
10% { opacity: 0.9; }
100% { transform: translateY(-900px) scale(1.3) rotate(18deg); opacity: 0; }
}

/* Rose container */
.rose-wrap {
width: 240px;
height: 300px;
margin: 0 auto;
position: relative;
transform: translateY(6px);
animation: popIn 1.2s ease forwards;
opacity: 0;
}

@keyframes popIn {
0% { transform: translateY(18px) scale(0.95); opacity: 0; }
100% { transform: translateY(6px) scale(1); opacity: 1; }
}

/* stem */
.stem {
position: absolute;
left: 50%;
bottom: 0;
width: 10px;
height: 170px;
transform: translateX(-50%);
background: linear-gradient(#2fe17a, #0a7a38);
border-radius: 999px;
box-shadow: 0 0 20px rgba(30, 255, 140, 0.15);
animation: sway 4s ease-in-out infinite;
transform-origin: bottom center;
}

/* leaves */
.leaf {
position: absolute;
width: 90px;
height: 50px;
background: linear-gradient(135deg, #34f38b, #0a7a38);
border-radius: 100px 100px 100px 0;
filter: drop-shadow(0 8px 14px rgba(0,0,0,0.12));
animation: leafPulse 3.5s ease-in-out infinite;
}

.leaf.left {
left: 40px;
bottom: 70px;
transform: rotate(-22deg);
}

.leaf.right {
right: 40px;
bottom: 95px;
transform: scaleX(-1) rotate(-18deg);
}

@keyframes leafPulse {
0%,100% { transform: translateY(0) rotate(-20deg); }
50% { transform: translateY(-4px) rotate(-24deg); }
}

/* flower head */
.flower {
position: absolute;
left: 50%;
top: 18px;
width: 180px;
height: 180px;
transform: translateX(-50%);
animation: sway 4s ease-in-out infinite;
transform-origin: 50% 80%;
}

@keyframes sway {
0%,100% { transform: translateX(-50%) rotate(-2deg); }
50% { transform: translateX(-50%) rotate(2deg); }
}

/* petals */
.petal {
position: absolute;
width: 110px;
height: 110px;
left: 50%;
top: 50%;
transform: translate(-50%, -50%);
border-radius: 70% 30% 60% 40%;
background: radial-gradient(circle at 30% 30%, #ffe0ef, #ff2f7a 60%, #a1003a);
filter: drop-shadow(0 14px 18px rgba(0,0,0,0.18));
animation: bloom 3.6s ease-in-out infinite;
transform-origin: center;
}

.petal:nth-child(1) { transform: translate(-50%,-50%) rotate(0deg); }
.petal:nth-child(2) { transform: translate(-50%,-50%) rotate(45deg); }
.petal:nth-child(3) { transform: translate(-50%,-50%) rotate(90deg); }
.petal:nth-child(4) { transform: translate(-50%,-50%) rotate(135deg); }
.petal:nth-child(5) { transform: translate(-50%,-50%) rotate(180deg); }
.petal:nth-child(6) { transform: translate(-50%,-50%) rotate(225deg); }
.petal:nth-child(7) { transform: translate(-50%,-50%) rotate(270deg); }
.petal:nth-child(8) { transform: translate(-50%,-50%) rotate(315deg); }

@keyframes bloom {
0%,100% { filter: drop-shadow(0 14px 18px rgba(0,0,0,0.18)); }
50% { filter: drop-shadow(0 18px 24px rgba(255, 80, 160, 0.20)); }
}

/* center bud */
.bud {
position: absolute;
left: 50%;
top: 50%;
width: 70px;
height: 70px;
transform: translate(-50%, -50%);
border-radius: 60% 40% 60% 40%;
background: radial-gradient(circle at 30% 30%, #fff0f7, #ff0f66 55%, #7b0028);
box-shadow: 0 0 30px rgba(255, 80, 160, 0.25);
animation: heartbeat 2.4s ease-in-out infinite;
}

@keyframes heartbeat {
0%,100% { transform: translate(-50%,-50%) scale(1); }
50% { transform: translate(-50%,-50%) scale(1.08); }
}

.big-msg {
margin-top: 14px;
font-size: 20px;
font-weight: 950;
color: rgba(50, 10, 30, 0.92);
letter-spacing: 0.2px;
animation: msgGlow 2.8s ease-in-out infinite;
}

@keyframes msgGlow {
0%,100% { transform: scale(1); opacity: 0.92; }
50% { transform: scale(1.02); opacity: 1; }
}

.small-msg {
margin-top: 8px;
font-size: 13px;
color: rgba(60, 20, 40, 0.62);
font-weight: 700;
}

.buttons {
margin-top: 18px;
display: flex;
justify-content: center;
gap: 14px;
flex-wrap: wrap;
position: relative;
}

.btn {
border: 0;
cursor: pointer;
padding: 12px 18px;
border-radius: 999px;
font-weight: 900;
font-size: 14px;
transition: transform 0.12s ease, opacity 0.12s ease;
user-select: none;
}

.btn:active { transform: scale(0.98); }

.yes {
background: rgba(255, 60, 140, 0.95);
color: white;
box-shadow: 0 16px 30px rgba(255, 60, 140, 0.20);
}

.no {
background: rgba(255,255,255,0.55);
color: rgba(60, 10, 30, 0.75);
border: 1px solid rgba(255, 105, 180, 0.22);
position: relative;
}

.final {
margin-top: 18px;
font-size: 18px;
font-weight: 950;
color: rgba(40, 10, 25, 0.9);
display: none;
line-height: 1.35;
}

.final span {
font-size: 22px;
}
</style>
</head>

<body>
<div class="blob one"></div>
<div class="blob two"></div>
<div class="blob three"></div>

<div class="card">
<h1>For you 🌹</h1>
<p>A rose that blooms every time you scan.</p>

<div class="rose-wrap" id="roseWrap">
<div class="flower">
<div class="petal"></div>
<div class="petal"></div>
<div class="petal"></div>
<div class="petal"></div>
<div class="petal"></div>
<div class="petal"></div>
<div class="petal"></div>
<div class="petal"></div>
<div class="bud"></div>
</div>

<div class="stem"></div>
<div class="leaf left"></div>
<div class="leaf right"></div>
</div>

<div class="big-msg">Will you be mine forever? 💖</div>
<div class="small-msg">Choose wisely 😌</div>

<div class="buttons">
<button class="btn yes" id="yesBtn">YES 💘</button>
<button class="btn no" id="noBtn">NO 🙃</button>
</div>

<div class="final" id="finalMsg">
<span>AAAAA 😭💖</span><br/>
You just made me the happiest person alive 🌹✨
</div>
</div>

<script>
// sparkles
const sparkleCount = 20;
for (let i = 0; i < sparkleCount; i++) {
const s = document.createElement("div");
s.className = "sparkle";
s.style.left = Math.random() * 100 + "vw";
s.style.top = (70 + Math.random() * 30) + "vh";
s.style.animationDelay = (Math.random() * 6) + "s";
s.style.animationDuration = (4 + Math.random() * 4) + "s";
s.style.opacity = 0.25 + Math.random() * 0.75;
document.body.appendChild(s);
}

// hearts
const hearts = ["💖","💘","💗","💕","🌸","💞"];
function spawnHeart() {
const h = document.createElement("div");
h.className = "heart";
h.textContent = hearts[Math.floor(Math.random() * hearts.length)];
h.style.left = Math.random() * 100 + "vw";
h.style.top = (75 + Math.random() * 25) + "vh";
h.style.fontSize = (16 + Math.random() * 18) + "px";
h.style.animationDelay = (Math.random() * 1.5) + "s";
h.style.animationDuration = (5 + Math.random() * 5) + "s";
h.style.opacity = 0.35 + Math.random() * 0.65;
document.body.appendChild(h);
setTimeout(() => h.remove(), 12000);
}
for (let i = 0; i < 14; i++) spawnHeart();

// click anywhere for extra hearts
document.body.addEventListener("click", () => {
for (let i = 0; i < 8; i++) spawnHeart();
const rose = document.getElementById("roseWrap");
rose.animate(
[
{ transform: "translateY(6px) scale(1)" },
{ transform: "translateY(6px) scale(1.03)" },
{ transform: "translateY(6px) scale(1)" }
],
{ duration: 500, easing: "ease-out" }
);
});

// YES button
const yesBtn = document.getElementById("yesBtn");
const finalMsg = document.getElementById("finalMsg");

yesBtn.addEventListener("click", (e) => {
e.stopPropagation();

for (let i = 0; i < 30; i++) spawnHeart();

finalMsg.style.display = "block";
yesBtn.textContent = "YES 💍";
yesBtn.style.transform = "scale(1.05)";

// disable NO after YES
const no = document.getElementById("noBtn");
no.style.opacity = "0.25";
no.style.pointerEvents = "none";
});

// NO button runs away
const noBtn = document.getElementById("noBtn");

function moveNoButton() {
const card = document.querySelector(".card");
const rect = card.getBoundingClientRect();

const maxX = rect.width - noBtn.offsetWidth - 30;
const maxY = rect.height - noBtn.offsetHeight - 30;

const x = Math.random() * maxX;
const y = Math.random() * maxY;

noBtn.style.position = "absolute";
noBtn.style.left = x + "px";
noBtn.style.top = y + "px";
}

noBtn.addEventListener("mouseenter", moveNoButton);
noBtn.addEventListener("touchstart", moveNoButton);
noBtn.addEventListener("click", moveNoButton);
</script>
</body>
</html>
